# Native Federation POC

## Installation
Just run the `install-all.sh` is the project root folder.

## Build
You can build every projects using `num run build`

## Start
You can start every projects using `num run start`
